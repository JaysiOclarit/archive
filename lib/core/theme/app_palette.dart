import 'package:flutter/material.dart';

class AppPalette {
  static const Color warmPaper = Color(0xFFFBF9F4);
  static const Color mossGreen = Color(0xFF546253);
  static const Color driedClay = Color(0xFF6A5D50);
  static const Color charcoal = Color(0xFF31332C);
  static const Color whitePaper = Color(0xFFFFFFFF);
  static const Color dimPaper = Color(0xFFD9DBCF);

  // You can also add transparent versions here
  static final Color charcoalSubtle = charcoal.withAlpha((0.15 * 255).round());
}
